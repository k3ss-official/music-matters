Music Matters: Exhaustive Audit of MLX and SOTA Audio Tools for Apple Silicon (M4)Strategic Architecture and Executive RecommendationsThe development of a local-first DJ and music production automation platform on Apple Silicon (M4) utilizing Python 3.11, FastAPI, and a React/Vite frontend represents a paradigm shift in computational audio engineering. The M4 system-on-a-chip, characterized by its high-bandwidth unified memory architecture and integrated Neural Engine, provides an unprecedented hardware foundation for executing deep learning models and digital signal processing (DSP) without the latency of traversing PCIe buses. The current technology stack establishes a robust baseline: demucs-mlx provides hardware-accelerated stem separation, allin1 and madmom handle generalized structural and rhythmic segmentation, while librosa, essentia, and scikit-learn form the bedrock of the DSP and feature extraction layers.However, advancing this pipeline to achieve state-of-the-art (SOTA) automation in 2026—specifically for complex DJ workflows encompassing harmonic mixing, semantic cue point detection, groove quantization, and high-fidelity polyphonic transcription—requires augmenting the existing stack. The contemporary machine learning landscape is evolving rapidly, pivoting away from traditional recurrent architectures toward diffusion transformers (DiTs), rotary position embeddings (RoPE), and vision-transformer-based spectrogram analysis.Following a comprehensive audit of the ml-explore ecosystem, community MLX repositories, and the broader Python PyTorch/MPS landscape, five tools emerge as transformative additions to the pipeline. These technologies bridge critical gaps in the existing architecture and are designated as mandatory integrations for achieving SOTA capabilities.Top Five "Install These Now" RecommendationsTool NameInstallation CommandCore CapabilityM4 Compatibility StatusArchitectural Justification and Complementary Valueaudio-separatorpip install "audio-separator[coreml]"Advanced SOTA Stem SeparationStable (CoreML / MPS / CPU)Wraps the Ultimate Vocal Remover (UVR) models. Crucially provides access to the BS-RoFormer model, which represents the undisputed 2024/2025 SOTA for source separation, outperforming the currently installed Demucs v4 architecture.cue-detrgit clone https://github.com/ETH-DISCO/cue-detrSemantic DJ Cue Point DetectionStable (MPS)Replaces brittle, rule-based energy thresholding algorithms with a pre-trained object detection transformer (DETR) fine-tuned on 21,000 professional DJ switch points, identifying drops and buildups with remarkable human-like intuition.mlx-audiopip install mlx-audioZero-Shot Text-Guided SeparationStable (Native MLX)Consolidates advanced audio transformer models into a native MLX library. Specifically, it introduces SAM-Audio, allowing for arbitrary, text-prompted audio isolation (e.g., "isolate the hi-hat") beyond standard fixed-stem taxonomies.BACHIVia HuggingFace TransformersSymbolic Chord RecognitionStable (MPS)The 2025 SOTA for chord recognition. Utilizes Masked Iterative Decoding to predict chord roots, qualities, and bass inversions, significantly outperforming legacy HMM-based tools like Chordino.Dejavupip install PyDejavuAudio Fingerprinting / DeduplicationStable (CPU / DSP)A pure Python implementation of peak-constellation hashing. Offers a highly robust, local-first database alternative to cloud-dependent services like AcoustID, enabling instant duplicate detection during track ingest.Native Apple Silicon Capabilities: The MLX Ecosystem AuditApple's MLX framework fundamentally alters the inference landscape for macOS by discarding the translation overhead associated with PyTorch's Metal Performance Shaders (MPS) backend. MLX utilizes lazy evaluation, dynamic graph construction, and a NumPy-compliant API that directly targets the M-series GPU via shared memory arrays. While demucs-mlx effectively harnesses this for 4-stem separation, a broad sweep of the github.com/ml-explore and mlx-community ecosystems reveals several highly relevant audio-adjacent repositories that address the specific search goals.MLX Ports: Vocal/Stem Separators, Melody, and Beat TrackingThe algorithmic translation from PyTorch to MLX is non-trivial, as MLX enforces channels-last layouts for convolutions and does not natively support complex numbers (requiring paired sine/cosine representations for operations like Rotary Position Embeddings). Consequently, an audit of the community reveals specific availability and absences regarding native MLX ports.Stem Separation (Beyond 4-Stem): While legacy architectures like Open-Unmix and Spleeter have not been formally ported to MLX—largely because their underlying separation fidelity has been eclipsed by modern transformers—the currently installed demucs-mlx repository actually possesses latent 6-stem capabilities. By invoking the htdemucs_6s model variant, the native MLX pipeline expands its output from the standard four stems (vocals, drums, bass, other) to explicitly include discrete piano and guitar stems. This 6-stem model is a variant of the Hybrid Transformer Demucs (v4) and operates with bit-exact parity to the PyTorch upstream, achieving inference speeds up to 73x faster than real-time on Apple Silicon.Melody Extraction (CREPE / BasicPitch): A comprehensive analysis of MLX repositories indicates that direct, optimized MLX translations of the CREPE (Convolutional Representation for Pitch Estimation) model or Spotify's BasicPitch neural network do not currently exist in the official ml-explore or mainstream community forks. The absence of an MLX CREPE port is likely due to the model's reliance on specific 1D convolutional heuristics that are adequately serviced by existing MPS backends.Beat and Tempo Tracking (BeatNet / Beat Transformer): Similarly, advanced rhythmic analyzers such as BeatNet (a Convolutional Recurrent Neural Network utilizing particle filtering) and Beat Transformer have not received dedicated MLX ports. The temporal nature of these RNN-based beat trackers often yields diminishing returns when ported to MLX compared to highly parallelizable transformer architectures. The optimal strategy remains executing these specific models via PyTorch MPS.Multi-Modal Audio Understanding and Generation via MLXDespite the absence of specific legacy ports, the MLX community has developed sophisticated multi-modal audio platforms that surpass the capabilities of dedicated single-task networks.Tool NameInstallation CommandCore CapabilityM4 Compatibility StatusJustification and Complementary ValueACE-Step-1.5git clone https://github.com/ACE-Step/ACE-Step-1.5.gitGenerative Audio & Music AnalysisExperimental (Native MLX)Represents a hybrid approach using a Qwen-based language model for musical planning and a Diffusion Transformer (DiT) for synthesis. Crucially, its audio understanding module can natively extract BPM, key, scale, and time signatures directly via MLX.mlx-audiopip install mlx-audioSpeech-to-Text & Source SeparationStable (Native MLX)Consolidates Whisper, Qwen3-ASR, and SAM-Audio. Provides zero-shot, prompt-based audio separation, filling the void left by fixed-stem models.musicgen-mlxgit clone https://github.com/andrade0/musicgen-mlxText-to-Music GenerationExperimental (Native MLX)An MLX port of Meta's autoregressive MusicGen. Capable of generating loop layers or background textures faster than real-time on the M4 Max.The inclusion of ACE-Step-1.5 is particularly notable for a DJ automation platform. While DSP libraries compute mathematical key profiles, the ACE-Step architecture utilizes intrinsic reinforcement learning to evaluate the semantic structure of a track. Passing an ingested loop through the ACE-Step understanding module yields a highly reliable, neural-net-derived estimation of the track's key, scale, and BPM, acting as a powerful verification layer against the algorithmic outputs of librosa or essentia.Furthermore, the mlx-audio package incorporates SAM-Audio (Segment Anything Model for Audio). This integration allows the FastAPI backend to execute arbitrary queries—such as isolating a specific synth patch or extracting an anomalous percussion hit—using natural language embeddings rather than relying strictly on the VDBO (Vocals, Drums, Bass, Other) paradigm.State-of-the-Art Non-MLX Tools: Pushing the Boundaries of MPS and CPUWhile MLX represents the frontier for Apple Silicon optimization, the broader Python ecosystem contains SOTA models published in 2024 and 2025 that leverage PyTorch's MPS backend or highly optimized C++ bindings to deliver unprecedented accuracy.SOTA Source Separation: Eclipsing DemucsThe current pipeline relies on Demucs v4 via demucs-mlx. While exceptionally fast, Demucs is no longer the pinnacle of audio separation fidelity. The industry SOTA has shifted toward sophisticated frequency-domain architectures.Tool NameInstallation CommandCore CapabilityM4 Compatibility StatusJustification and Complementary Valueaudio-separatorpip install "audio-separator[coreml]"BS-RoFormer / SCNet XL ExecutionStable (CoreML / MPS)Exposes the Ultimate Vocal Remover (UVR) model ecosystem programmatically. BS-RoFormer currently holds the highest Source-to-Distortion Ratio (SDR) on the MUSDB18-HQ benchmark.Banquetgit clone https://github.com/kwatcharasupat/query-banditQuery-Based Narrow Stem SeparationExperimental (PyTorch MPS)Separates non-standard instruments (reeds, organs, clean acoustic guitars) using a lightweight 24.9M parameter model linked to a PaSST recognition network.The audio-separator library is a mandatory inclusion for offline, high-fidelity loop rendering. The underlying Band-Split RoPE Transformer (BS-RoFormer) operates fundamentally differently from the U-Net architecture of Demucs. BS-RoFormer projects complex audio spectrograms into subband-level representations and sequences them through a hierarchical stack of transformers. To maintain temporal coherence across these subbands, it utilizes Rotary Position Embeddings (RoPE), culminating in an average SDR of 9.80 dB—vastly outperforming the phase alignment capabilities of Demucs. In practice, the FastAPI backend should route real-time, on-deck stem splitting to demucs-mlx for speed, while dispatching finalized loop extraction tasks to audio-separator running BS-RoFormer for pristine DAW export.Furthermore, the Banquet model addresses the inflexibility of fixed-stem decoders. By integrating a band-split model with a music instrument recognition network, Banquet allows the user to query specific, narrow instrument classes. This is highly complementary to the platform's loop editing pipeline, enabling the extraction of highly specific melodic elements without the computational overhead of monolithic multi-stem models.SOTA Chord RecognitionHistorically, chord recognition relied on template-matching algorithms analyzing chromagrams, exemplified by the C++ Chordino vamp plugin (wrapped in Python via chord-extractor) or deep learning approaches utilizing Bi-LSTM-CRF networks (such as the autochord package). However, these methodologies often falter in the presence of complex overtones and multi-instrumental masking, peaking at roughly 67% accuracy.Tool NameInstallation CommandCore CapabilityM4 Compatibility StatusJustification and Complementary ValueBACHIgit clone https://github.com/Itsuki-music/BACHI_Chord_RecognitionSymbolic and Audio Chord DetectionStable (PyTorch MPS)The 2025 SOTA for chord recognition. Decomposes detection into boundary alignment and Masked Iterative Decoding, achieving unprecedented accuracy on pop music benchmarks.chord-extractorpip install chord-extractorHigh-Speed HMM Chord DetectionStable (CPU C++)A fast, multiprocessing wrapper around the legacy Chordino algorithm. Serves as a low-latency fallback if the neural network overhead of BACHI is prohibitive.The BACHI (Boundary-Aware Symbolic Chord Recognition) framework represents a massive leap in harmonic analysis. Published in 2025, BACHI mirrors human ear-training paradigms by decoupling the analytical steps. It first establishes rigid temporal boundaries within the track and subsequently employs Masked Iterative Decoding to independently rank the chord root, the chord quality (major, minor, diminished), and the bass inversion. Executed via HuggingFace Transformers on the M4's MPS backend, BACHI provides the deep, granular harmonic metadata required for sophisticated loop editing and Ableton MIDI generation.Audio-to-MIDI TranscriptionThe current radar includes Spotify's BasicPitch, a lightweight neural network adept at capturing polyphonic pitch bends. However, when applied to complex, multi-instrumental loops, BasicPitch suffers from significant instrument leakage, wherein harmonic transients from a bassline might be erroneously transcribed as lower-octave piano notes.Tool NameInstallation CommandCore CapabilityM4 Compatibility StatusJustification and Complementary ValueNeuralNoteStandalone / C++ bindings via RTNeuralPolyphonic Audio-to-MIDIStable (CPU / MPS)Optimizes the BasicPitch model using RTNeural and ONNXRuntime for zero-latency inference. Integrates MR-MT3 methodologies to drastically reduce instrument leakage.NeuralNote provides a critical upgrade. While it utilizes the foundational BasicPitch architecture, it executes the Convolutional Neural Network (CNN) computations via the highly optimized RTNeural engine, and calculates the Constant-Q transform and harmonic stacking via ONNXRuntime. To address the instrument leakage inherent in BasicPitch, SOTA methodologies in 2024 leverage approaches like MR-MT3 (Memory Retaining Multi-Track Music Transcription), which introduces a memory retention mechanism across the temporal axis to enforce instrument continuity. Integrating NeuralNote into the platform's export pipeline ensures that the generated .mid files routed to Ableton are clean, quantized, and free of the fragmented "billion note" mess typical of older transcription algorithms.Automatic Music Structure Analysis and DJ Cue Point DetectionThe existing stack employs allin1 for structural segmentation, which relies on standard convolutional networks to label broad sections (intro, verse, chorus). While adequate for basic navigation, the requirements of a DJ automation platform necessitate hyper-specific semantic awareness—specifically, the precise moments where a track's energy dynamics allow for a seamless transition.Tool NameInstallation CommandCore CapabilityM4 Compatibility StatusJustification and Complementary Valuecue-detrgit clone https://github.com/ETH-DISCO/cue-detrSemantic DJ Switch Point DetectionStable (PyTorch MPS)Revolutionizes cue point detection by adapting a computer vision object detection transformer (DETR) to audio spectrograms. Trained on 21k human-annotated DJ cues.msafpip install msafSelf-Similarity Structural AnalysisStable (CPU)Provides deeper, mathematically rigorous boundary detection by analyzing structural repetition through self-similarity matrices, supplementing allin1.Semantic Cue Point Detection: Professional DJs do not merely mix on downbeats; they mix on "switch points"—the precise phrase boundaries where the dominant energy of one track yields to another, typically following a breakdown or build-up. Historically, automated systems attempted to locate these points by applying heuristic thresholds to novelty curves representing signal energy, timbre, and drum onsets. This rule-based peak-picking inevitably fails on complex EDM tracks with continuous low-end frequencies.The cue-detr (2024) repository fundamentally solves this. It frames cue point estimation not as a signal processing problem, but as a computer vision object detection task. By fine-tuning a Detection Transformer (DETR) on a massive dataset of 21,000 manually annotated cue points, the model learns the high-level phrasing and structural semantics of electronic dance music. It places bounding-box equivalents across the spectrogram, identifying the exact temporal boundaries for drops, build-ups, and emotional releases with over 90% human-parity accuracy.Structural Adaptation: For overarching verse/chorus labeling, the 2025 SOTA involves the temporal adaptation of pre-trained music foundation models. By analyzing self-similarity matrices—essentially plotting the track against itself to find horizontal lines indicating repeated sections—models can definitively isolate a recurring chorus from a unique bridge. The integration of msaf alongside cue-detr provides the platform with a comprehensive structural map, allowing the automation logic to confidently loop a 16-bar outro and mix it into the true drop of the incoming track.Fractional BPM, Groove Quantization, and Loop QualityStandard beat tracking algorithms, including those in librosa and madmom, frequently round tempos to whole integers or suffer from octave errors (halving or doubling the true BPM). Furthermore, they struggle to quantify the "feel" or swing of a rhythm.Tool NameInstallation CommandCore CapabilityM4 Compatibility StatusJustification and Complementary ValuePytakt / DeepRhythmNative PythonFractional Tempo EstimationStable (CPU)Calculates metrical units in fractional "ticks" rather than integers. DeepRhythm provides neural-net protection against octave errors.GrooveToolboxNative PythonSwing and Microtiming AnalysisStable (CPU)Extracts velocity matrices and microtiming deviations from audio, allowing the calculation of specific swing ratios and structural rhythm similarity.To achieve extreme fractional precision (e.g., distinguishing 124.3 BPM from 124.0 BPM), the optimal architectural approach involves executing Harmonic-Percussive Source Separation (HPSS) prior to beat tracking. By utilizing librosa.decompose.hpss to isolate the percussive transients, and subsequently passing only that transient data to the beat tracker, fractional accuracy increases significantly. Incorporating Pytakt, which measures time in fractional "ticks" (a 480th of a quarter note), provides the mathematical resolution required for precise DAW warping.Groove and Danceability Scoring: Evaluating the "quality" or "danceability" of a loop requires moving beyond simple downbeat detection. The GrooveToolbox library parses rhythmic patterns to extract microtiming deviations and velocity dynamics. By comparing an ingested drum loop against a perfectly quantized grid, the toolbox calculates a fuzzy Hamming distance and a specific "swing ratio". This data is invaluable for the loop editing pipeline; it allows the platform to extract the exact groove template from an ingested audio loop and apply that identical humanized swing to the MIDI data transcribed by NeuralNote.DJ-Specific Tooling: Automation Logic and Spectral MixingThe automation of DJ workflows requires sophisticated logic capable of executing harmonic transitions, automated phrase padding, and preventative EQing.Harmonic Mixing: Camelot Wheel and Open Key LogicThe foundation of modern DJing is harmonic mixing—blending tracks whose musical keys share identical or adjacent notes on the Circle of Fifths.Tool NameInstallation CommandCore CapabilityM4 Compatibility StatusJustification and Complementary ValueCamelot-Wheel-NotationAlgorithmic Translation (Python)Modulo 12 Harmonic LogicStable (CPU)Provides the state-machine logic for harmonic transitions. Essential for building the automated track recommendation engine.Harmonic mixing logic does not require deep learning; it relies on modulo 12 arithmetic applied to recognized keys. Once BACHI or ACE-Step extracts the musical key, the platform must map it to a Camelot code (1-12, A/B). The logic derived from repositories like Camelot-Wheel-Notation dictates the permissible transitions:Perfect Compatibility: Moving between identical keys (e.g., 5A to 5A).Adjacent Transition: Shifting ±1 on the numerical axis (e.g., 5A to 4A or 6A), representing a shift of a perfect fifth.Relative Mode Shift: Maintaining the number but flipping the mode (e.g., 5A minor to 5B major).Energy Boost: Jumping +2 on the numerical axis (e.g., 5A to 7A) to increase dancefloor energy.Embedding this logic into the FastAPI backend allows the platform to automatically traverse a user's library and generate harmonically seamless playlists.Phrase Matching and Automatic Mix PointsTool: MixingBearInstallation: pip install mixingbearM4 Compatibility: Stable (CPU)With the cue points identified by cue-detr, the platform must align the waveforms. MixingBear utilizes the AudioOwl library to calculate optimal synchronization points through cross-correlation and offset detection. It pads the beats of the incoming track, trims leading silences, and aligns the macro-phrasing (e.g., matching the 1st beat of a 16-bar phrase in track A to the 1st beat of a 16-bar phrase in track B). This ensures the automated mix respects the periodic 4/4 structural boundaries inherent in EDM.Spectral Analysis and Bass Clash DetectionA seamless transition requires preventing frequency masking—specifically the "bass clash" that occurs when the sub-frequencies of two simultaneous kick drums or basslines destructively interfere.Tool NameInstallation CommandCore CapabilityM4 Compatibility StatusJustification and Complementary Valueeulepip install euleSet-Theoretic Masking DetectionStable (CPU)Uses Venn-diagram mathematical sets applied to frequency bins to explicitly identify overlapping, clashing bands between two audio stems.HiFiScan / spectrumpip install hifiscan spectrumFIR Generation and PSD AnalysisStable (CPU)Generates phase-neutral finite impulse response (FIR) filters and Power Spectral Density estimates to programmatically resolve the clashes identified by eule.The resolution of frequency masking in an automated pipeline involves a two-step algorithmic process. First, the spectrum library calculates the Power Spectral Density (PSD) of the low-end stems (typically 20Hz - 200Hz). The eule library is then utilized to perform a set-theoretic analysis on these frequency bins. By treating the dominant frequencies of Track A and Track B as mathematical sets, eule identifies the exact intersection (the clashing overlap).Once the clash is quantified (e.g., an overlap at 55Hz), HiFiScan calculates the precise phase-neutral FIR correction required. The Python backend can then apply a targeted, programmatic dynamic EQ curve or ducking envelope to the outgoing track's bass stem, mimicking the actions of a professional DJ utilizing the mixer's low-end EQ knobs.Foundational DSP: The Essentia DependencyThe user requested specific documentation regarding the installation of essentia, a comprehensive C++ library for audio analysis and MIR, which notoriously presents compilation challenges on modern macOS due to strict Homebrew dependency chains.M4 Compatibility and Versioning:
As of July 2025, Essentia has resolved the compilation friction for Apple Silicon by releasing pre-compiled wheels for the arm64 architecture covering Python 3.9 through Python 3.13.Target Version: essentia-2.1b6.dev1389Target Wheel for Python 3.11: essentia-2.1b6.dev1389-cp311-cp311-macosx_15_0_arm64.whl.Installation Protocol:While pip install essentia will pull the correct ARM64 wheel, the underlying C++ binaries still require system-level dependencies. Ensure the M4 environment is configured via Homebrew:Bashbrew install pkg-config gcc readline sqlite gdbm freetype libpng eigen libyaml fftw ffmpeg@2.8 libsamplerate libtag chromaprint
pip install essentia==2.1b6.dev1389
This guarantees that Essentia operates with full hardware compatibility alongside librosa, providing the necessary speed for baseline spectral extraction and onset detection.Audio Fingerprinting and Duplicate DetectionAs the platform ingests massive volumes of audio files, identifying duplicates—even those that have been pitch-shifted, time-stretched, or subjected to light compression—is critical to preventing database bloat.Tool NameInstallation CommandCore CapabilityM4 Compatibility StatusJustification and Complementary ValueDejavupip install PyDejavuPeak Constellation HashingStable (CPU / Fast)A pure Python library utilizing local databases. Vastly superior to Chromaprint/AcoustID for localized, offline duplicate detection and sample recognition.While Chromaprint (AcoustID) relies on generating chromagrams optimized for matching near-identical full tracks against a global remote database, its architecture trades localized precision for search speed. For a local-first DJ platform, Dejavu is the superior architectural choice.Dejavu employs a mechanism popularized by Shazam: it utilizes a Fast Fourier Transform (FFT) to convert the audio into a spectrogram, applies a 2D maximum filter to locate the prominent time-frequency peaks (landmarks), and generates "constellation" hash keys based on the combinatorial relationships (timing and frequency Deltas) between those peaks. Because Dejavu relies on the relative distances between local energy maxima rather than the absolute waveform, it is extraordinarily robust against background noise and EQ modifications. It stores these hashes in a local SQLite or PostgreSQL instance, allowing the FastAPI backend to instantly cross-reference ingested loops against the user's entire local library.Integrated Pipeline Architecture: Synthesis and WorkflowTo seamlessly fuse these discrete MLX, PyTorch, and DSP technologies into the specified Python 3.11 / FastAPI / React stack, the architecture must implement a rigid separation of concerns, routing tasks based on their computational profile to either the real-time ingest loop or asynchronous background workers.1. Ingestion, Fingerprinting, and Feature Extraction (Real-Time)
Upon file upload via the React/Vite frontend, the FastAPI backend routes the audio byte-stream to Dejavu for instant duplicate verification against the local SQLite database. Simultaneously, librosa and Pytakt execute Harmonic-Percussive Source Separation to isolate transients, establishing a highly accurate, fractional BPM baseline. The track's basic metadata is immediately returned to the UI to populate the initial WaveSurfer.js waveform display.2. Deep Semantic and Harmonic Analysis (Asynchronous Workers)
The audio is subsequently dispatched to a queue (e.g., Celery) for deep neural analysis. The BACHI model executes Masked Iterative Decoding via the MPS backend to generate a granular, beat-aligned chord progression map. Concurrently, cue-detr parses the spectrogram, leveraging its DETR architecture to lay bounding boxes over the track's macro-structure, identifying the optimal energetic "switch points" for drops and buildups. The ACE-Step-1.5 MLX module processes a generalized semantic caption of the track's mood and instrumentation.3. Stem Separation and Loop Processing (On-Demand)
When the user triggers a stem isolation request in the UI, the backend initiates separation. For rapid, real-time previewing, the pipeline invokes the MLX-native demucs-mlx utilizing the htdemucs_6s flag, isolating vocals, drums, bass, other, piano, and guitar near-instantaneously via Apple Silicon's unified memory.
When the user commits to a final, high-fidelity export, the backend redirects the task to audio-separator, processing the loop through the SOTA BS-RoFormer model for pristine, artifact-free stem extraction.4. Automation, Mixing, and DAW Export
To construct an automated mix or remix, the Python logic applies the Camelot Wheel algorithms to identify harmonically compatible tracks. MixingBear utilizes the cue points established by cue-detr to calculate the exact sample offsets required for phrase alignment. During the transition window, the eule and HiFiScan libraries detect overlapping bass frequencies and programmatically apply phase-neutral FIR ducking curves to the outgoing track. Finally, the selected loops are processed through NeuralNote to generate accurate, instrument-leakage-free polyphonic MIDI , and all audio stems, MIDI files, and tempo maps are packaged into an Ableton Live Set (.als) for seamless export to the user's DAW.