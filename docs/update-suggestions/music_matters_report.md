# Music‑production/DJ tools audit for Apple M4 (March 2026)

## Overview

You are building a **local‑first DJ and music‑production platform** on an Apple M4 Mac using Python 3.11, FastAPI and a React/Vite front‑end.  This audit surveys the current landscape (late 2025 – early 2026) of audio analysis tools that could complement your pipeline: **ingestion → audio analysis → stem separation → loop editing → export to Ableton/DAW**.  The report assumes you already installed **Demucs‑MLX**, **allin1**, **madmom**, **librosa**, **soundfile/audioread**, **numpy/scipy/scikit‑learn**, **WaveSurfer.js** and **Essentia**.  Each section is grouped by task, with a summary table listing the tool name, install command, description, Apple Silicon (M4) compatibility, and comments on why it could be useful.  Experimental or unstable projects are flagged.  At the end, the **top recommendations** are highlighted.

## 1 MLX community models and Apple‑Silicon ports

| Tool | Install command | What it does | M4/MLX compatibility | Why it’s useful / differences vs existing tools |
|---|---|---|---|---|
| **`demucs‑mlx`** (Meta) | Already installed; skip reinstall. | Fast native **MLX** port of Demucs for 4–6‑stem music source separation.  It offers models `htdemucs`, `htdemucs_6s`, `mdx` etc., performs ~73× real‑time on M4 and includes a CLI (`demucs‑mlx track.wav`) and Python API【183193163436820†L263-L299】. | Runs directly on Apple Silicon without PyTorch; uses MLX; provides bit‑exact parity with upstream Demucs【183193163436820†L365-L374】. | Already part of your stack – basis for stem separation.
| **`mlx‑audio‑separator`** | `pip install mlx‑audio‑separator` (optionally `pip install 'mlx‑audio‑separator[convert]'` to convert weights)【84877361946070†L70-L99】 | New (2025) MLX port of the open‑source **audio‑separator** pipeline.  Supports multiple architectures (Roformer, MDX/MDXC, VR, Demucs) and separates audio into 4‑, 5‑, 6‑ or more sources.  CLI (`mlx‑audio‑separator song.mp3`) lists available models and provides options for fast/accurate inference【84877361946070†L0-L66】.  Includes a Python API: `from separator import Separator; sep = Separator(); sep.load_model('htdemucs_ft.yaml'); sources = sep.separate('song.mp3')`【84877361946070†L100-L113】. | Designed specifically for Apple Silicon and MLX, with macOS 13+, Python ≥3.10.  Benchmarks show ~1.8× speed‑up vs the PyTorch version on an M4 mini【84877361946070†L0-L66】. | Provides **six‑stem** (piano and guitar added) separation and other architectures; complements Demucs‑MLX when you need more than 4 stems or alternative models.
| **`pyannote‑segmentation‑3.0‑mlx`** | `pip install mlx numpy torchaudio` and then import `Segmentation` from the model card【897878076522977†L86-L111】 | MLX port of the Pyannote 3.0 speaker‑diarization model.  Processes raw audio and predicts speaker‑activity probabilities per frame using a SincNet + Bi‑LSTM architecture【897878076522977†L60-L83】.  Example usage: load waveforms, run `Segmentation.from_pretrained('pyannote/segmentation-3.0-mlx')(waveform)`. | Requires Apple Silicon; uses MLX for inference. | Provides **voice activity/diarization**; not core to DJ mixing but could be useful if you need to isolate vocals in podcasts or live‑sets.
| **`mlx‑audio` (SAM‑Audio)** | `pip install mlx‑audio` (or via `uv`)【629592189975337†L472-L504】 | Comprehensive MLX audio library providing speech‑to‑text, text‑to‑speech and **text‑guided source separation**.  For the latter, `SAMAudio.from_pretrained('mlx-community/sam-audio-large')` allows you to specify a text prompt (e.g. “guitar”, “vocals”) and separate that source from the mixture【629592189975337†L181-L194】. | Designed for Apple Silicon; uses MLX with quantization for fast inference.  Works on M4. | Interesting for creative tasks (separate “kick drum” by description) but still experimental; might help build personalised stems beyond fixed numbers.
| **`musicgen‑mlx`** | `git clone https://github.com/andrade0/musicgen-mlx` and `make install`【821571573867874†L45-L67】 | MLX port of Meta’s MusicGen for text‑to‑music generation.  CLI allows generating 8 seconds of music in ~6 s on an M4【821571573867874†L45-L67】. | Runs on M4. | Not directly needed for DJ mixing but shows the viability of generative music on Apple Silicon.

**Summary:** the MLX ecosystem is still young; aside from Demucs‑MLX and the new `mlx‑audio‑separator`, there are **no MLX ports** of chord recognition, key detection, beat tracking or melody extraction.  For those tasks we rely on Python libraries running on CPU/MPS.

## 2 Chord recognition and key detection

| Tool | Install command | What it does | M4 compatibility | Why it’s better or complementary |
|---|---|---|---|---|
| **`chord‑extractor`** (Chordino wrapper) | Requires libsndfile and `pip install chord‑extractor`【973392028224266†L300-L339】 | Wraps the Chordino vamp plugin to extract chord sequences from audio.  Use `from chord_extractor import Chordino; chords, timestamps = Chordino(roll=True).extract(audio)`【973392028224266†L300-L339】.  Supports concurrency via `extract_many`【973392028224266†L340-L372】. | CPU‑based; works on Apple Silicon because it uses Numpy and C libraries. | A simple baseline algorithm; useful when you need chord labels quickly.  However it uses conventional chromagram + HMM and may misidentify extended chords.
| **`pyace` (Automatic Chord Estimation)** | `pip install pyace`【905356874025721†L0-L32】 | Minimal Python implementation of Automatic Chord Estimation.  Supports major/minor triads using either a simple baseline or a deep RNN model.  Example: `pyace.simpleace(file.wav, result_path)` or `pyace.deepace(...)`【905356874025721†L34-L60】. | CPU‑only; uses Keras and hmmlearn; runs on M4. | Provides an alternative chord estimator; limited to 12 major/minor chords (no inversions).  Good for quick prototypes.
| **`skey` (Self‑supervised key detection)** | Clone the repo and run `poetry install`【262061757606031†L21-L53】; CLI: `poetry skey track.mp3 --device cpu` or use `detect_key(audio_dir, device='cpu')` in Python【262061757606031†L68-L88】. | State‑of‑the‑art self‑supervised model for musical key estimation from raw audio (ICASS‑SP 2025).  Supports `.wav`/`.mp3`, optional MPS/GPU, and returns key (A♯ minor etc.)【262061757606031†L21-L53】. | Runs on CPU, GPU and Apple‑MPS; tested on M1/M2; should work on M4. | More accurate than traditional key detectors (Krumhansl‑Schmuckler).  Provides both command‑line and Python API; ideal for harmonic mixing.
| **Essentia key detection** | Already installed via Essentia. | Essentia implements multiple key‑detection algorithms (Krumhansl, Temperley, EDM).  Use `KeyExtractor` and compare outputs. | CPU; works on M4. | Provides baseline and fallback algorithms; combine with S‑KEY for consensus.
| **Camelot wheel mapping** | Not a Python package; but the JS library `Camelot‑Wheel‑Notation` shows how to translate between standard key notation, Camelot numbers (1A/1B etc.) and propose harmonic movements【59546511039638†L9-L72】. | Use the mapping logic manually in Python; the library is JS. | Allows you to compute harmonic mixing suggestions (energy boost/drop etc.).  You can replicate the mapping logic using dictionaries.

**Notes:** there is no public MLX port of Open‑Unmix, Chordino or ACE.  For state‑of‑the‑art research you can explore **Chord transformer** models, but they require training; for now, `skey` plus Essentia provide strong key detection and `chord‑extractor`/`pyace` cover basic chord recognition.

## 3 Melody / pitch extraction and audio‑to‑MIDI transcription

| Tool | Install command | What it does | M4 compatibility | Why it’s better/complementary |
|---|---|---|---|---|
| **`CREPE` (TensorFlow)** | Install TensorFlow ≥ 2.0 and then `pip install crepe`【201262827422459†L92-L121】; CLI: `crepe track.wav` or use the Python API. | Deep CNN for **monophonic pitch tracking**.  Operates directly on raw audio and outperforms traditional methods.  CLI outputs time, frequency and confidence; Python API allows setting hop size and model capacity (tiny→full)【201262827422459†L127-L148】. | CPU or GPU; with Apple’s `tensorflow-metal` it can run on M-series GPUs; performance on M4 should be good. | High accuracy for monophonic instruments and vocals; can be used to extract melody lines for loop editing or to refine BasicPitch output.
| **`torchcrepe`** | `pip install torchcrepe`【34462301117229†L20-L63】 | PyTorch implementation of CREPE; supports CPU/GPU/MPS.  Example: `pitch = torchcrepe.predict(audio, sample_rate, hop_length, fmin, fmax, model='full', device='mps')`【34462301117229†L65-L96】. | MPS support means it runs natively on Apple Silicon. | Lower memory footprint than TensorFlow version; integrates easily with PyTorch pipelines; good for real‑time pitch tracking in your FastAPI backend.
| **`piano‑transcription‑inference`** | `pip install piano‑transcription‑inference`【632700853042778†L94-L102】; run `python3 example.py --audio_path song.mp3 --output_midi_path out.mid`【632700853042778†L108-L127】 | Toolbox from Bytedance implementing the *Onsets and Frames* high‑resolution piano transcription model.  Converts solo piano recordings to MIDI; includes a Python API `PianoTranscription(device).transcribe(audio, midi_path)`【632700853042778†L108-L127】. | Runs on CPU or GPU; the code notes Mac is supported but Windows untested【632700853042778†L138-L140】.  Should work on M4 with `torch` installed. | Provides **polyphonic piano** transcription; complementary to BasicPitch but limited to piano.  Useful for generating MIDI loops from piano stems.
| **`Omnizart`** | `pip install omnizart` then `omnizart download-checkpoints`【708602490339553†L114-L121】; CLI: `omnizart music transcribe song.wav` / `omnizart chord transcribe song.wav`【708602490339553†L114-L124】 | Comprehensive automatic music transcription toolbox.  Transcribes pitched instruments, chord progressions, drum events and beats【708602490339553†L99-L143】. | *Incompatible with ARM‑based macOS* due to dependencies【708602490339553†L148-L152】. | Not recommended for M4 users; included for completeness.  If you have an x86 machine or virtualization, it can provide full music transcription.
| **BasicPitch** (Spotify) | You already plan to use it; install via `pip install basic_pitch`. | Neural audio‑to‑MIDI converter for polyphonic signals; outputs pitch, onset and offset times.  Python API returns note events and pitch contours. | CPU or GPU; supports Apple M‑series via PyTorch with MPS. | Consider combining with CREPE for high‑confidence monophonic tracks and with piano‑transcription for piano‑only stems.

## 4 Beat, downbeat & tempo tracking

| Tool | Install command | What it does | M4 compatibility | Why it’s better/complementary |
|---|---|---|---|---|
| **`BeatNet`** | `pip install BeatNet` or `pip install git+https://github.com/mjhydri/BeatNet`【803966146195158†L66-L69】 | State‑of‑the‑art joint **beat, downbeat, tempo and meter tracking**.  Uses a CRNN neural network with particle filtering and supports four modes: streaming, real‑time, online and offline.  CLI can output beat times; Python API allows `BeatNet(online=True)` to get beat positions.  Offline mode uses BeatNet’s network with a DBN rather than madmom【803966146195158†L0-L18】【803966146195158†L116-L124】. | CPU and GPU; dependencies include librosa, pyaudio and madmom.  Works on Apple M4 via CPU; MPS currently unsupported but CPU is fast enough for offline processing. | Produces more robust beat/downbeat detection than madmom’s detectors and offers real‑time tracking; can replace or augment `madmom` for higher accuracy and fractional‑BPM output.
| **`Beat‑Transformer`** (research code) | clone the repo and train/test using provided scripts; requires dataset and Spleeter for demixing. | Transformer model for beat and downbeat tracking (ISMIR 2022).  Code is for training; inference requires manual setup. | Not packaged; heavy to set up; not recommended for production. | Included for completeness; treat as research if you plan to experiment with custom models.
| **Essentia `rhythm.bpm_extractor_multifeature`** | Already installed via Essentia. | Implements tempo estimation using combined onset strength and beat histogram.  Returns fractional BPM and beat positions. | CPU; runs on M4. | A baseline algorithm; use as fallback or when GPU resources are scarce.
| **`audioFlux`** | `pip install audioflux`【637493094242692†L409-L415】 | Comprehensive C/Python library providing dozens of time‑frequency transforms (BFT, CWT, CQT etc.) and features such as pitch, onset, hpss and chroma【637493094242692†L323-L394】. | Cross‑platform; has been tested on macOS, iOS and Android【637493094242692†L395-L400】. | Useful if you need low‑level spectral and MIR features; includes pitch and onset detectors and HPSS (harmonic/percussive separation) for custom beat analysis.

## 5 Source separation beyond four stems

| Tool | Install command | What it does | M4 compatibility | Why it’s better/complementary |
|---|---|---|---|---|
| **`mlx‑audio‑separator`** | See Section 1 above. | Provides **Roformer** and **MDX** models for 5‑/6‑stem separation (piano, guitar, drums, bass, other)【84877361946070†L70-L99】. | Apple Silicon/MLX only. | Ideal when Demucs‑MLX’s 4‑stem separation is insufficient.
| **`SAM‑Audio` (MLX‑Audio)** | Use `SAMAudio.from_pretrained('mlx-community/sam-audio-large')` and pass a text prompt (e.g. “lead vocals”)【629592189975337†L181-L194】. | Text‑guided separation using segment‑anything (audio).  Allows isolating arbitrary sources by description. | Apple‑Silicon; experimental; heavy memory usage. | Could be used for creative extraction of specific instruments (e.g., “hi‑hat”).  Not stable for production.
| **`HTDemucs` 6s / 5s models in Demucs‑MLX** | Already in `demucs‑mlx` via `--model htdemucs_6s` or `htdemucs_ft`. | Provide piano and guitar extra stems. | Works on M4; uses MLX. | Use as an alternative to the `mlx‑audio‑separator` if you prefer Demucs architecture.

## 6 Automatic music structure & cue‑point detection

| Tool | Install command | What it does | M4 compatibility | Why it’s better/complementary |
|---|---|---|---|---|
| **`allin1`** | Already installed. | Joint structural segmentation and downbeat detection; provides verse/chorus/bridge boundaries. | CPU (NumPy & Torch); works on M4. | Baseline for structure analysis.
| **`cue‑detr` (ISMIR 2024)** | Clone `ETH‑DISCO/cue‑detr` and run `python cue_points.py -t /path/to/audio/dir`【638356223880719†L265-L349】. | Machine‑learning approach to **cue‑point estimation**, treating cue points as objects in a detection task.  Works on EDM dataset; requires PyTorch and pre‑downloaded model checkpoints; CLI outputs `_cue_points.txt` with energy peaks, drops and build‑ups【638356223880719†L265-L349】. | CPU/GPU; should run on M4 via CPU.  Model is research‑grade and dataset specific. | Provides more advanced cue‑point detection than simple downbeat markers; useful for auto‑DJ algorithms but experimental.
| **`MixingBear`** | `pip install mixingbear`【22347660655961†L269-L278】 | Finds **mixing points** between two tracks and automatically beat‑mixes them.  Use `import mixingbear; mixingbear.mix('track1.wav','track2.wav','out.wav')`【22347660655961†L262-L266】.  Options allow selecting the first or random mix point and tuning the offset and silence trimming【22347660655961†L294-L305】. | CPU; depends on `ffmpeg` and `audioowl`; works on macOS (install `ffmpeg` via Homebrew)【22347660655961†L269-L273】. | Simple automatic beat‑mixing; can be integrated into your pipeline for crossfades or auditioning loops.
| **`transition‑analysis`** | Clone `mir‑aidj/transition‑analysis` and install dependencies (`pip install -r requirements.txt`)【718792132270273†L55-L63】. | Research code for **reverse‑engineering transition regions** in DJ mixes using sub‑band analysis and convex optimization【718792132270273†L24-L48】.  Not a library; requires step‑wise scripts (download, feature extraction, alignment, segmentation, reproduction). | CPU; uses madmom and cython; runs on M4 but heavy. | Provides insights into how DJs perform transitions; could inform custom algorithms.  Not ready for production.

## 7 Loop quality, danceability and energy scoring

| Tool | Install command | What it does | M4 compatibility | Why it’s better/complementary |
|---|---|---|---|---|
| **`pyCrossfade`** | Not on PyPI; clone `oguzhan‑yilmaz/pyCrossfade` and run via Docker or CLI.  The project provides a Docker image; example alias uses `docker run ghcr.io/oguzhan-yilmaz/pycrossfade:latest`【252804647116626†L41-L54】. | Creates **beat‑matched crossfades** with BPM time‑stretching and EQ curves; includes a CLI to extract BPM, key (via Essentia), **danceability score** and replay gain for a track (`pycrossfade extract track.mp3`)【252804647116626†L88-L107】.  Outputs features such as BPM (fractional), danceability (0–3), multiple key estimations and downbeat marking【252804647116626†L88-L107】. | Uses Essentia and FFMPEG within a Docker container.  Works on M4 via Docker but not pure Python. | Provides an out‑of‑the‑box tool for scoring tracks (danceability/energy) and performing crossfades; useful for ranking loops and estimating mix‑ability.
| **Essentia `danceability`** | Already installed; call `essentia.standard.Danceability` to compute a 0–1 score. | Essentia’s danceability algorithm uses rhythm features to estimate how suitable a track is for dancing (based on [Balbinot et al., 2013]). | CPU; runs on M4. | Use as a simple baseline for loop quality; results correlate with human judgement but can be noisy.
| **Custom features** | Not a library, but you can compute energy peaks, RMS, crest factor and spectral flux via `librosa` to detect drops and build‑ups. | Works on M4. | When no library gives a good “loop quality” score, designing features (energy variance, spectral centroid) may yield better heuristics.  Combine with machine‑learning (e.g., random forest) for loop scoring.

## 8 Audio fingerprinting and duplicate detection

| Tool | Install command | What it does | M4 compatibility | Why it’s useful |
|---|---|---|---|---|
| **`pyacoustid` (Chromaprint bindings)** | Install the Chromaprint library (via Homebrew) and then `pip install pyacoustid`【122352769085071†L94-L105】.  Use `acoustid.match(API_KEY, path)` to query AcoustID【122352769085071†L124-L135】. | Provides Python bindings for **Chromaprint** and AcoustID.  Generates audio fingerprints and queries the AcoustID web API to identify duplicates【122352769085071†L84-L88】.  Also exposes `fingerprint_file()` and `compare_fingerprints()` for local similarity【122352769085071†L137-L156】. | CPU; works on M4; depends on `ffmpeg`/`libavcodec`. | Standard open‑source fingerprinting; widely used in music libraries.  Good for duplicate detection, metadata lookup and verifying sample origins.
| **`Dejavu`** | Requires Python 3, a MySQL/Postgres database and optionally Docker; run `pip install dejavu` or clone the repo.  Then configure DB and call `djv.fingerprint_directory()`【667092551530811†L90-L120】. | Implements **audio fingerprinting and recognition** algorithm.  Fingerprints audio files, stores hashes in a database and matches unknown recordings against the database【667092551530811†L8-L14】【667092551530811†L90-L120】. | CPU; runs on M4; heavy fingerprinting can be parallelized. | Useful if you need an offline fingerprinting system without relying on AcoustID.  Good for detecting duplicates or verifying sample reuse.
| **`neural‑music‑fp`** (research) | Clone `raraz15/neural-music-fp`, install dependencies (TensorFlow 2.13, FAISS, Essentia) and run via Docker or Conda【527820020538462†L80-L107】. | Implements a **neural audio fingerprinting** system robust to audio degradation (ISMIR 2025).  Includes training and inference pipelines, FAISS database management and pre‑trained models【527820020538462†L11-L24】【527820020538462†L26-L39】. | Requires GPU for training; inference can run on CPU but heavy; not designed for M4. | Research‑level fingerprinting; not yet packaged for production but may outperform Chromaprint in noisy conditions.

## 9 Groove / swing quantisation detection

| Tool | Install command | What it does | M4 compatibility | Why it’s useful |
|---|---|---|---|---|
| **`GrooveToolbox`** | Clone `fredbru/GrooveToolbox`; dependencies include `pretty_midi`, `numpy`, `scipy`【837207234914132†L7-L20】. | Python toolbox for analysing **symbolic drum loops**.  Computes rhythm features and **micro‑timing features** (swing ratio, total density) and offers similarity metrics.  Example: `swingRatio = groove.microtimingFeatures.getSwingRatio()`【837207234914132†L27-L40】.  Also provides a script to load loops from audio via `LoadGrooveFromAudio.py` using ADTLib (only kick/snare/hihat)【837207234914132†L71-L76】. | CPU; runs on M4. | The only open‑source library for quantifying groove/swing; useful for analyzing loops and detecting swing patterns.  Limited to simple drum parts.
| **Custom detection using `librosa`** | Not a library; compute beat intervals and quantise them relative to the nearest grid. | Works on M4. | For arbitrary loops, computing inter‑beat intervals and measuring deviation from the grid can yield a swing metric.  Combine with `GrooveToolbox` for symbolic loops.

## 10 Audio‑to‑MIDI transcription and polyphonic tasks

See Section 3 for transcription.  In summary, **BasicPitch**, **piano‑transcription‑inference** and **CREPE/torchcrepe** cover most needs.  `Omnizart` is comprehensive but incompatible with ARM Mac; if you need chord transcription you could run it in an x86 container.

## 11 DJ‑specific tools (harmonic mixing, mix points, EQ/spectral analysis)

| Tool | Install command | What it does | M4 compatibility | Why it’s useful |
|---|---|---|---|---|
| **Harmonic mixing (Camelot wheel)** | Use custom Python code or port the JS library `Camelot‑Wheel‑Notation` which maps standard keys to Camelot notation and provides energy/mood transitions【59546511039638†L9-L76】. | Maps keys (e.g., C♯ minor → 12A) and suggests transitions such as energy boost (+1), drop (–1), mood change (+3) etc.【59546511039638†L50-L73】. | The JS library runs in browsers; you can replicate the mapping in Python using dictionaries. | Enables harmonic mixing suggestions for DJ sets; combine with key detection from S‑KEY or Essentia to pick compatible tracks.
| **`MixingBear`** | See Section 6. | Automatic beat‑mixing of two tracks; selects best mix point (random or first) and outputs cross‑mixed WAV【22347660655961†L262-L299】. | CPU; requires `ffmpeg`. | Provides basic auto‑DJ transitions; good for quick experiments or generating preview mixes.
| **`pyCrossfade`** | See Section 7. | CLI/ Docker tool performing beat‑matched crossfades with BPM/time‑stretch and EQ; outputs danceability, key and replay gain【252804647116626†L88-L107】. | Works via Docker on M4. | Great for assessing track energy and performing quick crossfades; includes Essentia‑based analysis.
| **EQ and spectral analysis** | Use `librosa`, `audioFlux` or `Essentia` to compute spectral centroid, bandwidth, harmonic/percussive separation, etc. | All run on M4. | You can implement **bass clash detection** by comparing low‑frequency energy between stems; **frequency masking** can be estimated by overlapping spectral densities.  No dedicated DJ‑focused library exists, so building your own analysis functions is the current approach.

## Top 3–5 recommendations to install now

1. **`mlx‑audio‑separator`** – expands stem separation beyond Demucs‑MLX by supporting Roformer, MDX and 5/6‑stem models.  Installation is straightforward (`pip install mlx‑audio‑separator`) and performance is excellent on M4【84877361946070†L0-L66】.

2. **`BeatNet`** – state‑of‑the‑art beat/downbeat/tempo tracker with fractional BPM support.  It improves upon madmom and provides real‑time/offline modes【803966146195158†L0-L18】.

3. **`skey`** – modern self‑supervised key detection that runs on CPU or Apple‑MPS.  Use it for accurate harmonic mixing; complement it with Essentia for robustness【262061757606031†L21-L53】.

4. **`CREPE`/`torchcrepe`** – high‑quality monophonic pitch trackers; integrate them with BasicPitch for better melody extraction【201262827422459†L92-L121】【34462301117229†L20-L63】.

5. **`pyCrossfade`** (via Docker) – quick way to compute danceability, BPM, key and perform beat‑matched crossfades【252804647116626†L88-L107】.  Useful for scoring loops and generating DJ mixes.

### Optional / research tools to explore

- **`cue‑detr`** for automatic cue‑point detection and energy‑drop detection (research grade)【638356223880719†L265-L349】.
- **`GrooveToolbox`** if you need to measure swing/groove in drum loops【837207234914132†L27-L40】.
- **`Dejavu`** or **`pyacoustid`** for audio fingerprinting and duplicate detection【667092551530811†L8-L14】【122352769085071†L94-L105】.
- **`piano‑transcription‑inference`** for high‑quality piano MIDI transcriptions【632700853042778†L108-L127】.
- **`mlx‑audio` (SAM‑Audio)** for text‑guided source separation if you need to isolate specific instruments via prompts【629592189975337†L181-L194】.

## Final remarks

The Apple‑Silicon MLX ecosystem now offers impressive performance for **source separation** and **speech processing**, but there are still **no MLX ports** for chord recognition, key detection, beat tracking or melody extraction.  For those tasks, rely on CPU‑ or MPS‑enabled Python libraries such as **BeatNet**, **skey** and **CREPE**.  Always check the licensing and maturity of each tool; research projects like `cue‑detr` and `neural‑music‑fp` may need adaptation before production use.  Combining several tools—Demucs‑MLX, BeatNet, S‑KEY, pyCrossfade and CREPE—will give your platform state‑of‑the‑art analysis capabilities on Apple M4.
