# Web Framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6
pydantic-settings==2.8.1
python-multipart==0.0.20

# Audio Processing
# NOTE: allin1 upgrades these; pinned versions below are minimums not maximums
librosa>=0.11.0
soundfile>=0.13.1
numpy>=2.4.3
scipy>=1.17.1

# MLX Analysis Stack (Step 1 — allin1 replaces librosa BPM/key + heuristic phrases)
# Install order matters:
#   pip install cython
#   pip install git+https://github.com/CPJKU/madmom --no-build-isolation
#   pip install allin1
# allin1 pulls in: torch, demucs, natten, madmom (transitive)
# natten_compat.py shim patches natten>=0.20 API for allin1 1.1.0.

# Stem Separation — Step 3: demucs-mlx (native Apple Silicon MLX, ~73× RT on M4)
#   pip install demucs-mlx
# Also installs: mlx, mlx-audio-io, mlx-metal, mlx-spectro

# Download & Metadata
yt-dlp==2025.1.15
httpx==0.27.2
musicbrainzngs==0.7.1
# Optional — uncomment if API keys are configured:
# spotipy==2.24.0
# python-discogs-client==2.3.0

# Utilities
python-dotenv==1.0.1
shazamio>=0.8.0
tqdm==4.66.5

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
